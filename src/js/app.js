/**
 * Next, we will create a fresh React component instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */

require("./index");

// chrome.commands.onCommand.addListener(function (command) {
// 	if (command === "bijoy") {
// 		// Set the user's keyboard in local storage
// 		chrome.storage.local.set({ keyboard: "bn" }, function () {
// 			console.log("Keyboard saved to local storage");
// 		});

// 		// Get the user's name from local storage
// 		chrome.storage.local.get("keyboard", function (result) {
// 			console.log(
// 				"Name retrieved from local storage: " + result.keyboard
// 			);
// 		});
// 	}
// });
